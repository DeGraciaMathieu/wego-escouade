{
  "name": "wego-escouade",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "npx serve .",
    "test": "node --test",
    "test:watch": "node --test --watch"
  }
}
